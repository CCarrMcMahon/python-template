# Changelog Fragments

This repository uses fragment-based changelog management. Rather than editing `CHANGELOG.md` directly during feature development, developers create modular fragment files within the `changes/` directory.
